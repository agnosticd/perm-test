---
name: Merge Gate

on:
  pull_request:
    types: [opened, synchronize, reopened]
  pull_request_review:
    types: [submitted, dismissed]

jobs:
  merge-gate:
    name: merge-gate
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
    steps:
      - name: Require maintainer approval
        uses: actions/github-script@v7
        with:
          script: |
            const prNumber = context.payload.pull_request.number;
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const prAuthor = context.payload.pull_request.user.login;

            // Get all reviews on this PR
            const { data: reviews } = await github.rest.pulls.listReviews({
              owner,
              repo,
              pull_number: prNumber,
            });

            // Build map of latest review per user
            const latestReviews = new Map();
            for (const review of reviews) {
              latestReviews.set(review.user.login, review);
            }

            // Check each approver's repo permission level
            // Maintain or Admin = maintainer, Write or below = contributor
            const validApprovals = [];
            for (const [login, review] of latestReviews) {
              if (review.state !== 'APPROVED') continue;
              if (login === prAuthor) continue;

              const { data: permData } = await github.rest.repos.getCollaboratorPermissionLevel({
                owner,
                repo,
                username: login,
              });

              if (['admin', 'maintain'].includes(permData.permission)) {
                validApprovals.push(login);
              }
            }

            if (validApprovals.length === 0) {
              core.setFailed(
                `❌ Requires approval from a user with Maintain or Admin permission before merging.\n` +
                `Contributors (Write permission) cannot satisfy this requirement.`
              );
            } else {
              core.info(`✅ Approved by maintainer(s): ${validApprovals.join(', ')}`);
            }
